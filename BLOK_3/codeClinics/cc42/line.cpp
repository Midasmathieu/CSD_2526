


function line(oldDelayTime, newDelayTime) {
  int stepSize = (newDelay\Time - oldDelayTime)/shiftTime;

  while(oldDelayTime = !newDelayTime) {
    oldDelayTime += stepSize;
    return oldDelayTime
  }
}
